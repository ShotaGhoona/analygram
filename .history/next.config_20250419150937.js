/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['scontent-itm1-1.cdninstagram.com', 'scontent.cdninstagram.com'],
    unoptimized: true,
  },
}

module.exports = nextConfig 